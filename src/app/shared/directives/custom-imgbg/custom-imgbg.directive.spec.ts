import { CustomImgbgDirective } from './custom-imgbg.directive';

describe('CustomImgbgDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomImgbgDirective();
    expect(directive).toBeTruthy();
  });
});

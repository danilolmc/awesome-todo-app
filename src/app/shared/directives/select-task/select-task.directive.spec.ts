import { SelectTaskDirective } from './select-task.directive';

describe('SelectTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectTaskDirective();
    expect(directive).toBeTruthy();
  });
});
